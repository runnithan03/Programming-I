from math import pi,tan
def point_on_ray(pt,ray):
    x=pt[0]
    y=pt[1]
    start_x=ray[0][0]
    start_y=ray[0][1]
    angle=ray[1]
    #either the point lies in part of the ray's line that does
    #not exist or it lies on the ray's line that does exist
print(tan(pi))